# Belvia mobile

Expo SDK 51 managed workflow. React Native 0.74. TypeScript strict.

Tabs: Trip · Schedule · Places · Bag · Tickets.

On-device only. Zustand + AsyncStorage. Directions open the system maps app so there is no tile API key.

```bash
cd mobile
npm install
npx expo start
```
